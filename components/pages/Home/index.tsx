import React, { FC } from "react";
import CarouselCard from "./CarouselCard";

const Home: FC<any> = (props) => {
  return (
    <React.Fragment>
      <CarouselCard />
    </React.Fragment>
  );
};

export default Home;
